// Data model lands in build-order step 2 (all 35 tables).
// This file only establishes the generator and datasource for now.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
